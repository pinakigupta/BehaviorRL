{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "RL",
            "type": "shell",
            "command": "python",
            "args": [
                "-W ignore",
                "${baselines_run}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}